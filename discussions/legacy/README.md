# 歷史討論封存區

最近封存日期：2026-07-25

這個資料夾保存目前問題地圖工作流程開始以前的討論內容。這些內容是歷史脈絡與假設來源，不是已經驗證的學術證據。

## 封存範圍

2026-07-25，原本直接放在 `discussions/` 下的既有項目全部移到這裡，沒有刪除任何內容。原先已封存的 `2026-07-13-deepfake-audio/` 保持不變。

舊文件中若出現以下路徑：

```text
discussions/<old-name>
```

現在對應的封存位置為：

```text
discussions/legacy/<old-name>
```

`exchange/claude-outbox/` 與 `exchange/codex-outbox/` 裡已經送出的交接檔案沒有改寫，因為 handoff 是不可變的歷史紀錄。其中的舊來源路徑應依照上述規則解讀。

## 目前討論

目前逐步進行的討論記錄於：

```text
discussions/problem-map/
```

第一版問題地圖因範圍展開過快，已封存於
`2026-07-25-problem-map-attempt-1/`，不再視為目前結論。
